#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <cstring>
#include <list>
#include <cmath>
using namespace std;

int dwarfs[9];

int main(void)
{
    int total = 0;
    int fake = 0;
    for(int i=0; i<9; i++) {
        scanf("%d", &dwarfs[i]);
        total += dwarfs[i];
    }
    sort(dwarfs, dwarfs + 9);
    
    for(int i=0; i<8; i++) {
        for(int j=i+1; j<9; j++) {
            fake = dwarfs[i] + dwarfs[j];
            if((total - fake) == 100) {
                dwarfs[i] = -1;
                dwarfs[j] = -1;
                break;
            }
        }
    }
    for(int i=0; i<9; i++) {
        if(dwarfs[i] != -1)  printf("%d\n", dwarfs[i]);
    }
    return 0;
}


